# 🛡️ VIBE SECURITY SUMMARY
# Otomatik üretildi: 25.02.2026 23:26:16
# Bu dosyayı AI araçlarına (Cursor, Copilot, vb.) context olarak verin.

## 📊 PROJE GÜVENLİK DURUMU
🚨 DURUM: KRİTİK — 11 kritik, 2 uyarı, 0 bilgi

## 📜 ZORUNLU GÜVENLİK KURALLARI

### 1. ÇEVRE DEĞİŞKENLERİ
- NEXT_PUBLIC_ öneki SADECE istemci tarafında güvenli değerler için kullanılır.
- SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL, *_SECRET, *_PRIVATE_KEY → ASLA NEXT_PUBLIC_ ile kullanılmaz.
- ⚠️ İFŞA EDİLEN: NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_DATABASE_URL, NEXT_PUBLIC_JWT_SECRET, NEXT_PUBLIC_API_SECRET, NEXT_PUBLIC_STRIPE_SECRET_KEY, NEXT_PUBLIC_SMTP_PASSWORD, NEXT_PUBLIC_SERPER_API_KEY, NEXT_PUBLIC_OPENAI_API_KEY

### 2. SUPABASE RLS
- Bu projede RLS ZORUNLUDUR.
- Her tabloda auth.uid() bazlı politikalar olmalıdır.

### 3. SQL INJECTION
- Template literal (${}) ile SQL OLUŞTURULMAZ.
- String concatenation (+) ile SQL OLUŞTURULMAZ.
- Parametreli sorgular ZORUNLUDUR.

### 4. API ANAHTARI GÜVENLİĞİ
- Serper.dev, OpenAI, Anthropic, Google AI → SADECE sunucu tarafında.
- İstemci tarafından erişim: API Route (/api/*) proxy ile.
- "use client" dosyalarında bu servislere doğrudan erişim YASAKTIR.

## 🏗️ MİMARİ
- Next.js App Router
- Hassas işlemler → Server Components, API Routes, Server Actions
- İstemci → Sadece UI rendering
- Veritabanı → RLS korumalı Supabase client
- Harici API → /api/* proxy

## 🚨 AKTİF SORUNLAR
🔴 [CRITICAL] 🚨 GÜVENLİK RİSKİ: 'NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY' istemci tarafına ifşa ediliyor! → .env.local:11
🔴 [CRITICAL] 🚨 GÜVENLİK RİSKİ: 'NEXT_PUBLIC_DATABASE_URL' istemci tarafına ifşa ediliyor! → .env.local:17
🔴 [CRITICAL] 🚨 GÜVENLİK RİSKİ: 'NEXT_PUBLIC_JWT_SECRET' istemci tarafına ifşa ediliyor! → .env.local:20
🔴 [CRITICAL] 🚨 GÜVENLİK RİSKİ: 'NEXT_PUBLIC_JWT_SECRET' istemci tarafına ifşa ediliyor! → .env.local:20
🔴 [CRITICAL] 🚨 GÜVENLİK RİSKİ: 'NEXT_PUBLIC_API_SECRET' istemci tarafına ifşa ediliyor! → .env.local:23
🔴 [CRITICAL] 🚨 GÜVENLİK RİSKİ: 'NEXT_PUBLIC_API_SECRET' istemci tarafına ifşa ediliyor! → .env.local:23
🔴 [CRITICAL] 🚨 GÜVENLİK RİSKİ: 'NEXT_PUBLIC_STRIPE_SECRET_KEY' istemci tarafına ifşa ediliyor! → .env.local:26
🔴 [CRITICAL] 🚨 GÜVENLİK RİSKİ: 'NEXT_PUBLIC_STRIPE_SECRET_KEY' istemci tarafına ifşa ediliyor! → .env.local:26
🔴 [CRITICAL] 🔴 Veritabanı Bağlantı Hatası: password authentication failed for user "user" → .env.local
🔴 [CRITICAL] 🔑 Serper.dev API anahtarı NEXT_PUBLIC_ ile ifşa ediliyor! → .env.local:35
🔴 [CRITICAL] 🔑 OpenAI API anahtarı NEXT_PUBLIC_ ile ifşa ediliyor! → .env.local:38
🟡 [WARNING] 🚨 GÜVENLİK RİSKİ: 'NEXT_PUBLIC_SMTP_PASSWORD' istemci tarafına ifşa ediliyor! → .env.local:32
🟡 [WARNING] 🚨 GÜVENLİK RİSKİ: 'NEXT_PUBLIC_SMTP_PASSWORD' istemci tarafına ifşa ediliyor! → .env.local:32

---
Güncelleme: npm run security | Config: vibe-security.config.js